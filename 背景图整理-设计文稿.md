# 背景图像索引 — Python 脚本设计文稿（修订版）

## 1. 项目目标

当前 Filter.json 只索引了 CG 图像，背景图像虽然在 `resource/` 目录中实际存在，但未被索引。本脚本的目标是为所有背景图像在 `Filter.json` 中自动创建索引条目。

---

## 2. 核心业务规则

### 2.1 背景图像的显示逻辑（前端）

- 背景图像**不是主要显示对象**。用户筛选某个章节或版本时，背景图像**不应出现**
- 只有用户**手动添加"背景图像"标签**时，才显示背景图像
- 实现方式：前端在基础查询阶段，若用户未勾选"背景图像"标签，则过滤掉所有带 `"背景图像"` 标签的条目
- **本条属于前端逻辑修改，不在本 Python 脚本的范围内，后续单独处理**

### 2.2 精确筛选交互

- 用户开启精确筛选 + 添加版本号/名称 + 添加"背景图像"标签 → 筛选出该版本对应的所有背景图像
- CG 图像没有"背景图像"标签，自动被精确筛选排除
- 精确/模糊筛选开关由前端现有逻辑管理，脚本无需关心

---

## 3. 版本边界定义

| 范围 | 版本 | 说明 |
|------|------|------|
| **1.4 之前** | 1.0, 1.1, 1.2, 1.3 | **不包括 1.4**。1.4 属于标准化版本 |
| **1.4 及之后** | 1.4 ~ 3.7 | 包括 1.4。官方标准化目录结构 |
| **特殊版本** | SP01 | 联动版本（跨越"翡冷翠之春"和"入雅典记"两个活动），目录名为 `s01/` |

---

## 4. 数据来源与目录结构

所有背景图像位于：

```
resource/singlebg/storybg/story_bg/
├── bg/           ← 1.4 之前的混装目录（CG + 背景图像混在一起，共 371 个文件）
├── 1_0_bg_*.png  ← 根目录零散文件（1.0~1.2 版本的少数背景图像）
├── 1_1_bg_*.png
├── 1_2_bg_*.png
├── 1.4/          ← 1.4 版本的背景图像（目录名使用点号）
├── 1_5/          ← 1.5 及之后使用下划线格式
├── 1_6/
├── .../
├── 3_7/
└── s01/          ← SP01 联动版本（15 个背景图像文件）
```

CG 目录（仅用于参考，不修改）：

```
resource/singlebg/storybg/story_atcg/
├── 1.4/          ← 各版本的 CG 文件
├── 1_5/
├── .../
├── 3_7/
└── s01/          ← SP01 联动版本 CG
```

---

## 5. 各范围的标签生成规则

### 5.1 1.4 之前 — 混装目录 `bg/`

**原理**：1.4 之前，CG 和背景图像放在同一个文件夹 `bg/` 中，无法按版本号分类。该目录中**已编入 Filter.json 的都是 CG 文件**，所以**除了 CG 文件以外的所有文件**就是背景图像。

**文件识别**：扫描 `bg/` 目录中的所有 `.png` 文件，排除：
- 已在 Filter.json 中出现的文件（通过 file 路径匹配）
- 文件名包含 `zone` 的文件（切图，应排除）

**标签规则**：
- 版本号：统一标记为 `"1.0"`
- 活动类型：无法区分主线/活动，**不写**
- 章节/活动名称：无法区分，**不写**
- 年份：**不写**
- 角色/人物：**不写**（程序批量生成，不包含人物标签）
- `"背景图像"` 标签：**写上**
- tone：新条目统一留空为 `[]`，由用户自有程序后续生成。对于已在 Filter.json 中的条目，保留其现有 tone 数据不变

**最终标签示例**：
```json
{
    "file": "resource/singlebg/storybg/story_bg/bg/0kanabi.png",
    "tags": ["1.0", "背景图像"],
    "tone": []
}
```

> **注意**：`tone` 字段由用户自有程序后续生成，脚本只保留空数组 `[]`，不预填任何色调数据。

### 5.2 1.4 之前 — 根目录零散文件

根目录下恰好有 3 个零散背景图像文件，**按各自对应的正规版本打标签**：

| 文件名 | 版本号 |
|--------|--------|
| `1_0_bg_kanabijie.png` | `"1.0"` |
| `1_1_bg_chaerdunjia.png` | `"1.1"` |
| `1_2_bg_muwumenwai_ye.png` | `"1.2"` |

这 3 个文件各自对应不同版本，数量极少。除了版本号之外，不写活动类型、章节名称、年份、人物，只带 `"背景图像"` 标签。`tone` 留空由后续程序填充。

**排除规则**：若文件已在 Filter.json 中出现，跳过。经检查，这 3 个文件均未入库，全部需要新增。

**可选方案**：由于数量极少（仅 3 个），若脚本不便处理，也可以手动补录。

### 5.3 1.4 及之后 — 版本化目录

**目录命名**：`story_bg/{版本目录}/`，版本目录名有两种格式：
- 点号格式：`1.4/` → 版本号 `"1.4"`
- 下划线格式：`1_5/` → 版本号 `"1.5"`

脚本统一转换为点号格式（`"X.Y"`）作为标签。

**每个版本目录中**，排除：
- 文件名包含 `zone` 的文件（切图）
- 已在 Filter.json 中出现的文件（已有背景图像不做处理）

**标签生成**：
1. **版本号**：从目录名提取并转换为点号格式
2. **活动类型**：拿着版本号在 Filter.json 中查询，取该版本对应的类型（`"所有主线"` 或 `"所有活动"`）。一个版本必然只有一种类型（要么主线，要么活动），忽略轶事条目
3. **版本名称**：拿着版本号在 Filter.json 中查询，取**非轶事**条目的版本名称。若同一版本有多个名称，取出现次数最多的那个（轶事标题不能代表整个版本）
4. **`"背景图像"`**：写上
5. **不写的标签**：年份、人物/角色（程序批量生成，不包含这些）
6. **tone**：新条目统一留空为 `[]`，由用户自有程序后续生成。已在 Filter.json 中的背景图像条目保留其现有 tone 数据不变

**最终标签示例**：
```json
{
    "file": "resource/singlebg/storybg/story_bg/3_5/3_5_bg_28.png",
    "tags": ["3.5", "所有活动", "《绿松石蛇俱乐部》", "背景图像"],
    "tone": []
}
```

> **注意**：`tone` 字段由用户自有程序后续生成，脚本只保留空数组 `[]`。

### 5.4 特殊版本 — SP01（`s01/` 目录）

- 目录名：`s01/`（共 15 个背景图像文件，`s01_bg_01.png` ~ `s01_bg_15.png`）
- **版本标签**：`"SP01"`
- **活动类型**：经确认，Filter.json 中所有 SP01 条目均使用 `"所有活动"` → **确定为 `"所有活动"`**
- **版本名称**：Filter.json 中 SP01 条目涉及两个活动名称——`"翡冷翠之春"`（14 条）和 `"入雅典记"`（12 条）。脚本应取出现次数最多的名称，即 **`"翡冷翠之春"`**
  - 原因：SP01 是一个特殊版本，跨越了"翡冷翠之春"和"入雅典记"两个活动。背景图像无法区分具体属于哪个活动，故统一使用最常见名称
- 同样不写年份、人物，`tone` 留空 `[]`，排除 zone 文件（`s01/` 目录经查无 zone 文件），排除已有条目（经查 s01 背景图像均未入库）

---

## 6. 插入位置策略

### 6.1 问题描述

Filter.json 中同一版本的条目**可能不是连续排列的**。例如 1.0 版本的条目分布在两个块中（第一块 [0, 25]，第二块 [108, 179]）。因此不能简单地在"版本最后一条"之后插入。

### 6.2 聚类插入策略

采用**版本群分类**方法：

1. **扫描 Filter.json**，为每个条目标注所属版本（取 tags 中首个 `X.Y` 格式的标签，或 `SP01`）
2. **对每个版本的条目位置进行聚类**，找出该版本在文件中的"密集分布区块"
3. **排序所有版本区块**，按区块中心位置排序
4. **确定插入位置**：对于要插入的版本 X 的新条目，找到版本 X 的最后一个区块的结束位置（`end_X`），以及下一个版本的第一个区块的开始位置（`start_next`），在 `[end_X, start_next]` 范围内选择一个位置插入

### 6.3 实际数据分析结果

经分析，Filter.json（共 1205 条）的实际情况：

| 区域 | 特征 |
|------|------|
| 1.0 ~ 1.3 | 1.0 横跨两块 [0,25] 和 [108,179]，中间嵌入 1.1/1.2/1.3。1.0 内部最大间隔达 83 |
| 1.4 ~ 3.7 | **严格顺序排列，完全无重叠**。每个版本的条目连续分布，范围首尾相接 |
| SP01 | 位于 [897, 922]，在 3.0 和 3.1 之间。版本顺序上 SP01 在 3.7 之后，但在文件中它被手动插在 3.0 之后 |

**结论**：1.4 之后的插入非常简单（版本间直接取间隙）。1.4 之前的插入需要聚类分析确定 1.0 的"第二区块"边界。SP01 条目已有，只需插入新的 SP01 背景图像到现有 SP01 区块附近。

### 6.4 实现建议

可使用 Python 的 `numpy` 或 `scipy.cluster` 进行简单的密度聚类，也可用纯 Python 的 gap-detection（间隙 > 阈值即视为区块边界）。考虑到版本数量有限（28 个），**纯 Python 实现已足够**，无需引入重依赖。

---

## 7. JSON 格式要求

### 7.1 正确格式（保持原样）

```json
{
    "file": "resource/singlebg/storybg/story_bg/3_5/3_5_bg_28.png",
    "tags": ["3.5", "所有活动", "《绿松石蛇俱乐部》", "背景图像"],
    "tone": ["饱和度-高", "对比度-弱", "明暗调-低", "色温-冷色", "色彩-单调"]
}
```

### 7.2 错误格式（禁止换行展开数组）

```json
{
    "file": "resource/singlebg/storybg/story_bg/3_5/3_5_bg_28.png",
    "tags": [
        "3.5",
        "所有活动",
        "《绿松石蛇俱乐部》",
        "背景图像"
    ],
    "tone": [
        "饱和度-高",
        "对比度-弱",
        "明暗调-低",
        "色温-冷色",
        "色彩-单调"
    ]
}
```

**要求**：Tags 和 tone 数组必须保持在一行内，使用 `separators=(',', ':')` 紧凑输出，避免 JSON 文件行数膨胀。

---

## 8. 文件操作规范

1. **创建 `test/` 文件夹**，将现有 `Filter.json` 复制到 `test/` 中进行测试
2. **不要直接修改**原 `Filter.json`，待测试验证通过后再替换
3. 涉及 `resource/` 文件夹的读取操作是允许的，但**禁止修改** `resource/` 中的任何文件
4. 脚本应输出处理统计信息：扫描了多少文件、新增了多少条目、排除了多少 zone 文件、跳过了多少已有条目

---

## 9. 现有背景图像条目参考

Filter.json 中已有 22 条背景图像条目（覆盖版本 1.3, 1.8, 2.0, 2.3, 2.6, 3.5, 3.6）。这些条目中**部分包含人物标签**（如 `"哑谜"`, `"霍夫曼"`），这是手动标记的，与本脚本自动生成的条目（不带人物标签）存在不一致，**属于已知差异，不做处理**。

脚本在判断"文件是否已存在"时，应该通过 file 路径精确匹配，不依赖标签内容。

---

## 10. 实现步骤概要

1. 加载 `Filter.json` → 构建已索引文件路径集合、版本→{类型, 名称}映射表
2. 扫描 `story_bg/bg/` 混装目录（1.4 前） → 排除 zone + 排除已索引 → 生成 1.0 标签条目
3. 扫描 `story_bg/` 根目录零散文件 → 按文件名前缀匹配版本 → 生成条目
4. 扫描 `story_bg/1.4/` ~ `story_bg/3_7/`（1.4 及之后） → 排除 zone + 排除已索引 → 从映射表查类型和名称 → 生成条目
5. 扫描 `story_bg/s01/`（SP01） → 排除 zone + 排除已索引 → 生成 SP01 条目
6. 按聚类策略确定每个新条目的插入位置
7. 将新条目插入 Filter.json 数据结构
8. 按紧凑格式写回 JSON 文件（`separators=(',', ':')`，`ensure_ascii=False`）
9. 输出统计报告

---

## 11. 待确认项（已全部评估完成）

- [x] **SP01 的活动类型**：确认为 `"所有活动"`。Filter.json 中全部 26 条 SP01 条目均使用 `"所有活动"`，无例外。
- [x] **SP01 的版本名称**：取 Filter.json 中 SP01 条目出现次数最多的名称，即 `"翡冷翠之春"`（14 次 vs `"入雅典记"` 12 次）。SP01 跨越两个活动，背景图无法区分归属，统一用最常见名称。
- [x] **聚类插入策略**：1.4 及之后版本严格顺序排列、完全无重叠，直接取版本间隙插入即可。1.4 之前（1.0~1.3）需处理 1.0 跨两个区块 [0,25] 和 [108,179] 的情况，纯 Python gap-detection 足够，无需 numpy/scipy。详见第 6 节。
- [x] **根目录其他零散文件**：经检查，`story_bg/` 根目录仅有 3 个零散文件（`1_0_bg_kanabijie.png`、`1_1_bg_chaerdunjia.png`、`1_2_bg_muwumenwai_ye.png`），无其他零散文件。数量极少，脚本可直接按文件名前缀匹配版本号，也可手动补录。
- [x] **tone 字段**：新条目的 `tone` 字段统一留空为 `[]`，由用户自有程序后续填充。已索引的 22 条背景图像条目保留其现有 tone 数据不变。
- [x] **s01/ 目录文件数**：共 15 个背景图像文件（`s01_bg_01.png` ~ `s01_bg_15.png`），全部未入库，无 zone 文件需排除。

---

## 12. 最终确认的条目格式

所有新增背景图像条目统一使用以下格式（程序会自动填充 tone 字段）：

```json
{
    "file": "resource/singlebg/storybg/story_bg/版本目录/文件名.png",
    "tags": ["版本号", "活动类型（若可确定）", "版本名称（若可确定）", "背景图像"],
    "tone": []
}
```

**各范围具体标签：**

| 范围 | 版本号 | 活动类型 | 版本名称 | 其他 |
|------|--------|----------|----------|------|
| `bg/` 混装目录 | `"1.0"` | 不写 | 不写 | 仅 `"背景图像"` |
| 根目录零散文件 | 各自版本 | 不写 | 不写 | 仅 `"背景图像"` |
| 1.4 ~ 3.7 版本目录 | 从目录名提取 | 从 Filter.json 查 | 从 Filter.json 查（取最频繁） | 仅 `"背景图像"` |
| `s01/` (SP01) | `"SP01"` | `"所有活动"` | `"翡冷翠之春"` | 仅 `"背景图像"` |

---

## 13. 预计产出统计

| 来源 | 文件总数 | 排除 zone | 已入库 | 预计新增 |
|------|----------|-----------|--------|----------|
| `bg/` 混装目录 | 371 | 32 | 180（CG 文件已入库） | **159** |
| 根目录零散 | 3 | 0 | 0 | **3** |
| 1.4 ~ 3.7 版本目录 | 584 | 0 | 7（已有背景条目散布各版本） | **481** |
| `s01/` (SP01) | 15 | 0 | 0 | **15** |
| **合计** | **973** | **32** | **187** | **658** |
