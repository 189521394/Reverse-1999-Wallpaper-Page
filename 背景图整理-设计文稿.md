# 背景图索引 — Python 脚本设计文稿（修订版）

## 1. 项目目标

当前 Filter.json 只索引了 CG 图像，背景图虽然在 `resource/` 目录中实际存在，但未被索引。本脚本的目标是为所有背景图在 `Filter.json` 中自动创建索引条目。

---

## 2. 核心业务规则

### 2.1 背景图的显示逻辑（前端）

- 背景图**不是主要显示对象**。用户筛选某个章节或版本时，背景图**不应出现**
- 只有用户**手动添加"背景图"标签**时，才显示背景图
- 实现方式：前端在基础查询阶段，若用户未勾选"背景图"标签，则过滤掉所有带 `"背景图"` 标签的条目
- **本条属于前端逻辑修改，不在本 Python 脚本的范围内，后续单独处理**

### 2.2 精确筛选交互

- 用户开启精确筛选 + 添加版本号/名称 + 添加"背景图"标签 → 筛选出该版本对应的所有背景图
- CG 图像没有"背景图"标签，自动被精确筛选排除
- 精确/模糊筛选开关由前端现有逻辑管理，脚本无需关心

---

## 3. 版本边界定义

| 范围 | 版本 | 说明 |
|------|------|------|
| **1.4 之前** | 1.0, 1.1, 1.2, 1.3 | **不包括 1.4**。1.4 属于标准化版本 |
| **1.4 及之后** | 1.4 ~ 3.7 | 包括 1.4。官方标准化目录结构 |
| **特殊版本** | SP01 | 联动版本"入雅典记"，目录名为 `s01/` |

---

## 4. 数据来源与目录结构

所有背景图位于：

```
resource/singlebg/storybg/story_bg/
├── bg/           ← 1.4 之前的混装目录（CG + 背景图混在一起，共 371 个文件）
├── 1_0_bg_*.png  ← 根目录零散文件（1.0~1.2 版本的少数背景图）
├── 1_1_bg_*.png
├── 1_2_bg_*.png
├── 1.4/          ← 1.4 版本的背景图（目录名使用点号）
├── 1_5/          ← 1.5 及之后使用下划线格式
├── 1_6/
├── .../
├── 3_7/
└── s01/          ← SP01 联动版本（15 个背景图文件）
```

CG 目录（仅用于参考，不修改）：

```
resource/singlebg/storybg/story_atcg/
├── 1.4/          ← 各版本的 CG 文件
├── 1_5/
├── .../
├── 3_7/
└── s01/          ← SP01 联动版本 CG
```

---

## 5. 各范围的标签生成规则

### 5.1 1.4 之前 — 混装目录 `bg/`

**原理**：1.4 之前，CG 和背景图放在同一个文件夹 `bg/` 中，无法按版本号分类。该目录中**已编入 Filter.json 的都是 CG 文件**，所以**除了 CG 文件以外的所有文件**就是背景图。

**文件识别**：扫描 `bg/` 目录中的所有 `.png` 文件，排除：
- 已在 Filter.json 中出现的文件（通过 file 路径匹配）
- 文件名包含 `zone` 的文件（切图，应排除）

**标签规则**：
- 版本号：统一标记为 `"1.0"`
- 活动类型：无法区分主线/活动，**不写**
- 章节/活动名称：无法区分，**不写**
- 年份：**不写**
- 角色/人物：**不写**（程序批量生成，不包含人物标签）
- `"背景图"` 标签：**写上**
- tone：由现有程序生成，脚本只保留 `tone` 字段，不修改其内容

**最终标签示例**：
```json
{
    "file": "resource/singlebg/storybg/story_bg/bg/0kanabi.png",
    "tags": ["1.0", "背景图"],
    "tone": ["饱和度-高", "对比度-弱", "明暗调-中", "色温-中性"]
}
```

### 5.2 1.4 之前 — 根目录零散文件

根目录下的零散背景图文件（`1_0_bg_*.png`、`1_1_bg_*.png`、`1_2_bg_*.png`），**按各自对应的正规版本打标签**：

| 文件名前缀 | 版本号 |
|-----------|--------|
| `1_0_bg_` | `"1.0"` |
| `1_1_bg_` | `"1.1"` |
| `1_2_bg_` | `"1.2"` |

这些文件仅 3 个，比较特殊。除了版本号之外，不写活动类型、章节名称、年份、人物，只带 `"背景图"` 标签。

**排除规则**：若文件已在 Filter.json 中出现，跳过。

### 5.3 1.4 及之后 — 版本化目录

**目录命名**：`story_bg/{版本目录}/`，版本目录名有两种格式：
- 点号格式：`1.4/` → 版本号 `"1.4"`
- 下划线格式：`1_5/` → 版本号 `"1.5"`

脚本统一转换为点号格式（`"X.Y"`）作为标签。

**每个版本目录中**，排除：
- 文件名包含 `zone` 的文件（切图）
- 已在 Filter.json 中出现的文件（已有背景图不做处理）

**标签生成**：
1. **版本号**：从目录名提取并转换为点号格式
2. **活动类型**：拿着版本号在 Filter.json 中查询，取该版本对应的类型（`"所有主线"` 或 `"所有活动"`）。一个版本必然只有一种类型（要么主线，要么活动），忽略轶事条目
3. **版本名称**：拿着版本号在 Filter.json 中查询，取**非轶事**条目的版本名称。若同一版本有多个名称，取出现次数最多的那个（轶事标题不能代表整个版本）
4. **`"背景图"`**：写上
5. **不写的标签**：年份、人物/角色（程序批量生成，不包含这些）
6. **tone**：保留现有程序生成的 tone 数据，不修改

**最终标签示例**：
```json
{
    "file": "resource/singlebg/storybg/story_bg/3_5/3_5_bg_28.png",
    "tags": ["3.5", "所有活动", "《绿松石蛇俱乐部》", "背景图"],
    "tone": ["饱和度-高", "对比度-弱", "明暗调-低", "色温-冷色", "色彩-单调"]
}
```

### 5.4 特殊版本 — SP01（`s01/` 目录）

- 目录名：`s01/`
- **版本标签**：`"SP01"`
- **版本名称**：`"入雅典记"`
- **活动类型**：拿着 `"SP01"` 在 Filter.json 中查询已有的 SP01 条目，取对应的类型（主线或活动）。如果查不到，需人工确认
- 同样不写年份、人物，保留 tone，排除 zone 文件，排除已有条目

---

## 6. 插入位置策略

### 6.1 问题描述

Filter.json 中同一版本的条目**可能不是连续排列的**。例如 1.0 版本的条目分布在两个块中（第一块 [0, 25]，第二块 [108, 179]）。因此不能简单地在"版本最后一条"之后插入。

### 6.2 聚类插入策略

采用**版本群分类**方法：

1. **扫描 Filter.json**，为每个条目标注所属版本（取 tags 中首个 `X.Y` 格式的标签，或 `SP01`）
2. **对每个版本的条目位置进行聚类**，找出该版本在文件中的"密集分布区块"
3. **排序所有版本区块**，按区块中心位置排序
4. **确定插入位置**：对于要插入的版本 X 的新条目，找到版本 X 的最后一个区块的结束位置（`end_X`），以及下一个版本的第一个区块的开始位置（`start_next`），在 `[end_X, start_next]` 范围内选择一个位置插入

### 6.3 实际数据分析结果

经分析，Filter.json（共 1205 条）的实际情况：

| 区域 | 特征 |
|------|------|
| 1.0 ~ 1.3 | 1.0 横跨两块 [0,25] 和 [108,179]，中间嵌入 1.1/1.2/1.3。1.0 内部最大间隔达 83 |
| 1.4 ~ 3.7 | **严格顺序排列，完全无重叠**。每个版本的条目连续分布，范围首尾相接 |
| SP01 | 位于 [897, 922]，在 3.0 和 3.1 之间。版本顺序上 SP01 在 3.7 之后，但在文件中它被手动插在 3.0 之后 |

**结论**：1.4 之后的插入非常简单（版本间直接取间隙）。1.4 之前的插入需要聚类分析确定 1.0 的"第二区块"边界。SP01 条目已有，只需插入新的 SP01 背景图到现有 SP01 区块附近。

### 6.4 实现建议

可使用 Python 的 `numpy` 或 `scipy.cluster` 进行简单的密度聚类，也可用纯 Python 的 gap-detection（间隙 > 阈值即视为区块边界）。考虑到版本数量有限（28 个），**纯 Python 实现已足够**，无需引入重依赖。

---

## 7. JSON 格式要求

### 7.1 正确格式（保持原样）

```json
{
    "file": "resource/singlebg/storybg/story_bg/3_5/3_5_bg_28.png",
    "tags": ["3.5", "所有活动", "《绿松石蛇俱乐部》", "背景图"],
    "tone": ["饱和度-高", "对比度-弱", "明暗调-低", "色温-冷色", "色彩-单调"]
}
```

### 7.2 错误格式（禁止换行展开数组）

```json
{
    "file": "resource/singlebg/storybg/story_bg/3_5/3_5_bg_28.png",
    "tags": [
        "3.5",
        "所有活动",
        "《绿松石蛇俱乐部》",
        "背景图"
    ],
    "tone": [
        "饱和度-高",
        "对比度-弱",
        "明暗调-低",
        "色温-冷色",
        "色彩-单调"
    ]
}
```

**要求**：Tags 和 tone 数组必须保持在一行内，使用 `separators=(',', ':')` 紧凑输出，避免 JSON 文件行数膨胀。

---

## 8. 文件操作规范

1. **创建 `test/` 文件夹**，将现有 `Filter.json` 复制到 `test/` 中进行测试
2. **不要直接修改**原 `Filter.json`，待测试验证通过后再替换
3. 涉及 `resource/` 文件夹的读取操作是允许的，但**禁止修改** `resource/` 中的任何文件
4. 脚本应输出处理统计信息：扫描了多少文件、新增了多少条目、排除了多少 zone 文件、跳过了多少已有条目

---

## 9. 现有背景图条目参考

Filter.json 中已有 22 条背景图条目（覆盖版本 1.3, 1.8, 2.0, 2.3, 2.6, 3.5, 3.6）。这些条目中**部分包含人物标签**（如 `"哑谜"`, `"霍夫曼"`），这是手动标记的，与本脚本自动生成的条目（不带人物标签）存在不一致，**属于已知差异，不做处理**。

脚本在判断"文件是否已存在"时，应该通过 file 路径精确匹配，不依赖标签内容。

---

## 10. 实现步骤概要

1. 加载 `Filter.json` → 构建已索引文件路径集合、版本→{类型, 名称}映射表
2. 扫描 `story_bg/bg/` 混装目录（1.4 前） → 排除 zone + 排除已索引 → 生成 1.0 标签条目
3. 扫描 `story_bg/` 根目录零散文件 → 按文件名前缀匹配版本 → 生成条目
4. 扫描 `story_bg/1.4/` ~ `story_bg/3_7/`（1.4 及之后） → 排除 zone + 排除已索引 → 从映射表查类型和名称 → 生成条目
5. 扫描 `story_bg/s01/`（SP01） → 排除 zone + 排除已索引 → 生成 SP01 条目
6. 按聚类策略确定每个新条目的插入位置
7. 将新条目插入 Filter.json 数据结构
8. 按紧凑格式写回 JSON 文件（`separators=(',', ':')`，`ensure_ascii=False`）
9. 输出统计报告

---

## 11. 待确认项（明天继续评估）

- [ ] SP01 的"活动类型"（主线还是活动）——需从 Filter.json 已有 SP01 条目确认
- [ ] 聚类插入策略的具体算法选择（纯 Python vs numpy/scipy）
- [ ] 是否需要处理 `story_bg/` 根目录下除 `1_0_bg_*`、`1_1_bg_*`、`1_2_bg_*` 以外的其他零散文件
- [ ] tone 字段：现有背景图的 tone 是由什么程序生成的？新条目是否需要先占位 tone 再运行该程序？
